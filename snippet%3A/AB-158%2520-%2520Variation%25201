document.documentElement.dataset.webAb158 = "1";

// window.addEventListener('load', () => {

    const edrGridContainer = document.querySelector('edr-boost-offers-grid');
    const otherOffersSection = document.querySelector('#other-offers');
    const productOffersSection = document.querySelector('#product-offers');
    const noBoostsSection = document.querySelector('section:has(edr-no-boost-offers)');

    if (edrGridContainer) {
      
      /*
        if (noBoostsSection && otherOffersSection) {
            edrGridContainer.insertBefore(otherOffersSection, noBoostsSection);
        } else if (otherOffersSection && productOffersSection) {
            edrGridContainer.insertBefore(otherOffersSection, productOffersSection);
        }
       */
      const topSection = noBoostsSection || productOffersSection;
      edrGridContainer.insertBefore(otherOffersSection, topSection);
    }


// }, false);